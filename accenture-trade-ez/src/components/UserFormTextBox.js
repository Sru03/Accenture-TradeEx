import React from 'react';

const UserFormTextBox = () => {
    return (
        <input
          type="text"
          className="rounded-textbox"
        />
      );
    };

export default UserFormTextBox;